# Maths

Inline: $e^{i\pi} + 1 = 0$.

Display:

$$
\int_{0}^{\infty} e^{-x^2}\,dx = \frac{\sqrt{\pi}}{2}
$$

A second display block, to prove each renders as one expression rather than a stack of
block boxes:

$$
\sum_{n=1}^{\infty} \frac{1}{n^2} = \frac{\pi^2}{6}
$$

An environment the converter does not support. It must show its source in place and must
not blank the page:

$$
\begin{aligned}
a &= b + c \\
d &= e + f
\end{aligned}
$$
