# Far below the fold

The image at the top loads immediately; the one at the bottom must not, until it is
scrolled towards. That only holds if `loading="lazy"` is in the markup the parser
first sees — set from JavaScript after `innerHTML`, it arrives after the fetch has
already begun and does nothing at all.

![above the fold](assets/pixel.png)

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. Filler paragraph, one of many, whose only job is to push the image below it far past any distance a browser is willing to preload. 

![far below the fold](assets/wide-screenshot.png)
