[package]
name = "kbview-docx"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
quick-xml = "0.42.0"
thiserror = "2.0.20"
zip = "8.6.0"

[dev-dependencies]
# Tests build real .docx packages: a .docx is a zip of XML parts.
zip = "8.6.0"
